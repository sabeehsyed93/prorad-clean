{
  "name": "prorad-api",
  "version": "1.0.0",
  "description": "Radiology Transcription API",
  "main": "index.js",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
